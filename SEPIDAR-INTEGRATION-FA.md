# راهنمای اتصال باشگاه مشتریان به سپیدار

نسخه 3.0 پروژه به صورت **باشگاه مشتریان مستقل** تنظیم شده است. مسیرهای CRM فروش، سرنخ، پیگیری، پروژه و شبکه نمایندگان از Runtime حذف شده‌اند.

## معماری
سپیدار منبع حقیقت مالی است و باشگاه به صورت Read-only مشتریان و فاکتورهای فروش را دریافت می‌کند. امتیاز، سطح، کیف پول، پاداش، کمپین و Retention فقط در باشگاه مدیریت می‌شوند.

## پیش‌نیاز سپیدار
1. وب‌سرویس سپیدار فعال باشد.
2. سپیدار در حالت سرویس‌دهنده اجرا شود (پورت پیش‌فرض 7373).
3. دستگاه Integration در سپیدار رجیستر شده باشد.
4. IntegrationID، نام کاربری دستگاه و Public Key تولیدشده در اختیار باشد.

## متغیرهای محیطی
در `crm-backend/.env`:

```
SEPIDAR_BASE_URL=http://127.0.0.1:7373
SEPIDAR_GENERATION_VERSION=112
SEPIDAR_INTEGRATION_ID=1001
SEPIDAR_USERNAME=...
SEPIDAR_PASSWORD=...
SEPIDAR_PUBLIC_KEY_XML=<RSAKeyValue>...</RSAKeyValue>
```

## همگام‌سازی
از پنل مدیر وارد `اتصال به سپیدار` شوید:
- تست اتصال
- همگام‌سازی مشتریان
- همگام‌سازی فاکتورها
- همگام‌سازی کامل

برای جلوگیری از ثبت تکراری، مشتری با `accountingRef` و فاکتور با `externalRef` ذخیره می‌شود.

## Migration دیتابیس
قبل از اجرای نسخه 3.0 migration زیر اعمال شود:
`crm-backend/prisma/migrations/20260911_loyalty_only_sepidar/migration.sql`

## سیاست امنیتی
باشگاه به طور پیش‌فرض چیزی در سپیدار ثبت یا ویرایش نمی‌کند. دسترسی مستقیم به دیتابیس سپیدار نیز استفاده نشده است.
